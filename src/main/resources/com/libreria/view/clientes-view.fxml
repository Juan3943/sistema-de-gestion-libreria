<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.libreria.controller.ClienteController">

    <!-- Barra superior: filtros y búsqueda -->
    <top>
        <VBox spacing="15" style="-fx-padding: 20; -fx-background-color: #f8f9fa;">

            <!-- Título -->
            <Label text="👥 Gestión de Clientes"
                   style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>

            <!-- Controles -->
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Button fx:id="btnNuevoCliente" text="+ Nuevo Cliente"
                        onAction="#abrirDialogoNuevoCliente"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 10 20;"/>

                <Separator orientation="VERTICAL"/>

                <Label text="Estado:"/>
                <ComboBox fx:id="cbFiltroEstado" prefWidth="120"/>

                <Separator orientation="VERTICAL"/>

                <Label text="Buscar:"/>
                <TextField fx:id="txtBuscar" promptText="Nombre o CUIT" prefWidth="200"/>
                <Button fx:id="btnBuscar" text="🔍 Buscar" onAction="#buscarClientes"
                        style="-fx-background-color: #3498db; -fx-text-fill: white;"/>
                <Button fx:id="btnActualizar" text="🔄 Actualizar" onAction="#cargarClientes"
                        style="-fx-background-color: #95a5a6; -fx-text-fill: white;"/>
            </HBox>
        </VBox>
    </top>

    <!-- Tabla de clientes -->
    <center>
        <TableView fx:id="tablaClientes" style="-fx-padding: 10;">
            <columns>
                <TableColumn fx:id="colTipo" text="Tipo" prefWidth="100"/>
                <TableColumn fx:id="colNombre" text="Nombre / Razón Social" prefWidth="250"/>
                <TableColumn fx:id="colCuit" text="CUIT" prefWidth="120"/>
                <TableColumn fx:id="colCondicionIva" text="Condición IVA" prefWidth="150"/>
                <TableColumn fx:id="colTelefono" text="Teléfono" prefWidth="120"/>
                <TableColumn fx:id="colEstado" text="Estado" prefWidth="80"/>
                <TableColumn fx:id="colAcciones" text="Acciones" prefWidth="200"/>
            </columns>

            <placeholder>
                <VBox alignment="CENTER" spacing="10">
                    <Label text="No hay clientes registrados"
                           style="-fx-font-size: 14px; -fx-text-fill: gray;"/>
                    <Label text="Haga clic en '+ Nuevo Cliente' para comenzar"
                           style="-fx-text-fill: gray;"/>
                </VBox>
            </placeholder>
        </TableView>
    </center>

</BorderPane>